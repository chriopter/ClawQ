CLAWQ_PASSWORD=replace-with-strong-password
# Optional: repos root (defaults to /root/git, falls back to /root/workspaces)
# CLAWQ_REPOS_ROOT=/root/git
# Memory README defaults to /root/workspaces/README.md
# CLAWQ_README_PATH=/root/workspaces/README.md
# Hooker Signal targets are configured in the UI (no env vars required)
# Optional: set your own signing secret (recommended)
# CLAWQ_COOKIE_SECRET=replace-with-long-random-string
# Keep false if TLS is terminated at reverse proxy
CLAWQ_COOKIE_SECURE=false
